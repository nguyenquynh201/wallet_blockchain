enum ValidateEmailState {
  none,
  invalid,
  notCorrect,
}

enum ValidateState {
  none,
  invalid,
  notCorrect,
}

enum ValidatePasswordState {
  none,
  invalid,
  notCorrect,
  short,
}

enum ValidatePhoneState {
  none,
  invalid,
  notCorrect,
}

enum ValidateCoinState {
  none,
  invalid,
  notCorrect,
}
